import Typography from "@mui/material/Typography";

const Logo = () => {
  return (
    <Typography variant="h5" color="primary" fontWeight="bold">
      Loops Document Viewer
    </Typography>
  );
};

export default Logo;
